apiVersion: v1
kind: Service
metadata:
  name: {{ include "server-app.fullname" . }}
  labels:
    {{- include "server-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "server-app.selectorLabels" . | nindent 4 }}
